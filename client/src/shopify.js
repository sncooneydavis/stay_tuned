// Shopify Buy SDK — handles cart state and product display on the client.
// Search queries go through the Express server proxy (server/src/routes.js) instead,
// because the Storefront API search requires a server-side call to avoid exposing credentials.
